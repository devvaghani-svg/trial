<header class="custom-header">
  <div class="header-container">

    <!-- Logo -->
    <div class="header-logo">
      <a href="{{ routes.root_url }}">

        {% if section.settings.logo != blank %}
          <img 
            src="{{ section.settings.logo | image_url }}" 
            alt="{{ shop.name }}"
            style="max-width: {{ section.settings.logo_width }}px;"
          >
        {% else %}
          <span>{{ shop.name }}</span>
        {% endif %}

      </a>
    </div>

    <!-- Navigation -->
    <nav class="header-nav">
      <ul>
        {% for link in section.settings.menu.links %}
          <li>
            <a href="{{ link.url }}">{{ link.title }}</a>
          </li>
        {% endfor %}
      </ul>
    </nav>

    <!-- Right Icons -->
    <div class="header-icons">

      <a href="/search">
        <img src="{{ 'images/search.png' | asset_url }}" alt="search">
      </a>

      <a href="/account">
        <img src="{{ 'images/account.png' | asset_url }}" alt="account">
      </a>

      <a href="/cart">
        <img src="{{ 'images/cart.png' | asset_url }}" alt="cart">
      </a>

    </div>

  </div>
</header>


{% schema %}
{
  "name": "Custom Header",
  "settings": [
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Header Logo"
    },
    {
      "type": "range",
      "id": "logo_width",
      "label": "Logo Width",
      "min": 80,
      "max": 300,
      "step": 10,
      "default": 150
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Navigation Menu",
      "default": "main-menu"
    }
  ]
}
{% endschema %}